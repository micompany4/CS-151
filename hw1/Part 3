
import java.util.*;

public class Encoding
{
	
   public static Set<String> morseCodes(int m, int n)
   {
      Set<String> result = new TreeSet<>();
   
      int total = m+n;
      String morse = "";
      for(int i = 0; i < n; i++)
      {
    	  morse+= "-";
      }
      for(int j = 0; j < m; j++)
      {
    	  morse += ".";
      }
      
      result.add(morse);
      
      return result;
   }
}
